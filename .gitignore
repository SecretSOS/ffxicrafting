# Python
__pycache__/
*.pyc
.venv/
venv/

# A LandSandBoat checkout, if you clone it inside the repo
lsb-server/
server/

# Local working notes — kept on disk for tooling context, never published.
# If these were already committed, untrack them:
#   git rm --cached CLAUDE.md docs/handoff.md
CLAUDE.md
docs/

# Local scratch
*.log
.DS_Store
Thumbs.db

# Uncomment if the database gets too large for comfort and you would rather
# rebuild it than commit it. It is ~8 MB, which git handles fine.
# data/ffxi_crafting.db
